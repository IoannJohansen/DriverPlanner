<UserControl x:Class="Driver_Planner.Views.Pages.ProfilePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Driver_Planner.Views.Pages"
             xmlns:fa5="http://schemas.fontawesome.com/icons/" 
             xmlns:viewmodels="clr-namespace:Driver_Planner.ViewModels"
             xmlns:viewModels="clr-namespace:DriverPlanner.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ProfileViewModel}"
             mc:Ignorable="d" 
             Height="510"
             Width="700"
             d:DesignHeight="510" 
             d:DesignWidth="700">

    <Grid Background="{StaticResource WorkingSpaceBG}">
        <ContentControl Content="{Binding CurrentProfileMode}"/>

    </Grid>
    
    
    
</UserControl>
